const getters = {
  navigationopen (state) {
    return state.navigationopen
  },
  title (state) {
    return state.title
  },
  pageanimation (state) {
    return state.pageanimation
  }
}
export default getters
